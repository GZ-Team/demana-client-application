mutation savePrinterConfiguration($printerConfigurationForm: PrinterConfigurationForm) {
    savePrinterConfiguration(printerConfigurationForm: $printerConfigurationForm) {
        automatic
        paperWidth
        paperMargin
    }
}
